server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/identity_service"
    username: "postgres"
    password: "admin"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true